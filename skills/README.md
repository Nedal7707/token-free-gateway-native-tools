# Project skills for the token-free gateway repo.

Drop skill folders here (each with a SKILL.md). These are loaded by the
project-level opencode config (`.opencode/opencode.jsonc` -> `skills.paths`).
Keep them generic and free of personal data.
